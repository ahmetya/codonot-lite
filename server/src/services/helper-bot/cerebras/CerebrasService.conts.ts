export const CHARACTER_CONTENT_PROMPT = `Generate one RPG character draft matching the supplied JSON schema.
Return exactly 3 personality items, 2 motivations, 2 flaws, and between 3 and
6 equipment items. Every item in those arrays must be a non-empty string.
Write background as a vivid 120-180 word origin story with homeland, a formative
event, current goal, and active conflict. Do not return a one-word or phrase-only
background.`;
